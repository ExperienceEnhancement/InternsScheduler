'use strict';

app.factory('calendar', function (requester) {
    var getHolidays = function (month) {
        var holidays = [
            
        ];
        
        return holidays;
    };

    return {
        holidays: getHolidays
    }
});
